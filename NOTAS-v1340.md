# v1340 — correção da dependência do portão de publicação

Correção mínima sobre a v1339.

- Move `acorn` de `devDependencies` para `dependencies`.
- Mantém a exigência já prevista pelo teste `v1338-portao-da-publicacao`.
- Evita que a suíte do build falhe em ambientes que instalam apenas dependências de produção.
- Nenhuma regra comercial, prompt, interface ou banco foi alterado.

Versão: 7.1340.0 / display 1340.
