# v757 — Correção de deploy: logos antigos removidos do build

Correção do erro do Vercel por arquivo de logo antigo exigido no build.

Mudanças:
- removidos os logos antigos da lista obrigatória do `build.js`;
- removidos os mesmos assets antigos do cache do `service-worker.js`;
- `styles.css` passa a usar somente `logo-cp.png`;
- versão atualizada para 757.

Não altera IA, análise, importação ou regras comerciais.
